import React from 'react'

const Header = () => {
  return (
    <header className='flex ai_fe sb'>
      <img alt='' src={require('../img/reload.png')} />
      <h1>Pet’s friends</h1>
      <img alt='' src={require('../img/settings.png')} />
    </header>
  )
}

export default Header
